# Run Memgraph Lab
podman run -d -p 3001:3000 --name lab memgraph/lab

# Run Memgraph 
podman run -p 7687:7687 -p 7444:7444 --name memgraph memgraph/memgraph

# Run Podman for Memegraph code
podman run --rm -it --net host --shm-size 22g  --device nvidia.com/gpu=all -v $(pwd)/:/home/genai -e DISPLAY=$DISPLAY -w /home/genai localhost/run_memgraph:v1 

# Run Deepstream
podman run --rm -it --net host --shm-size 22g  --device nvidia.com/gpu=all -v $(pwd)/:/home/genai -e DISPLAY=$DISPLAY -w /home/genai nvcr.io/nvidia/deepstream:7.0-gc-triton-devel

# Kafka Podman
podman run --rm -it --net host docker.io/apache/kafka-native:latest
